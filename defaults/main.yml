---
# defaults file for ansible-role-nessus

nessus_version: 'latest'
nessus_arch: 'amd64'
nessus_distro: 'debian' # this is for kali
nessus_lic: ''
nessus_install_dir: '/opt/nessus'
nessus_cli: '{{ nessus_install_dir }}/sbin/nessuscli'
